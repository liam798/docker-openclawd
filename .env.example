# OpenClaw Docker 环境变量示例
# 复制为 .env 并按需修改: cp .env.example .env

# ---------- 镜像与构建 ----------
# 镜像名（不设则用 openclaw:local，由本仓库 Dockerfile 构建）
# OPENCLAW_IMAGE=openclaw:local
# 构建时使用的 OpenClaw 版本（分支或 tag，如 main、v2026.1.30）
OPENCLAW_VERSION=main
# 构建时额外安装的 apt 包，空格分隔（如 ffmpeg build-essential）
# OPENCLAW_DOCKER_APT_PACKAGES=

# ---------- 目录（宿主机路径） ----------
# 配置与状态目录（会挂载到容器 ~/.openclaw）
OPENCLAW_CONFIG_DIR=./data/openclaw
# 工作区目录（会挂载到容器 ~/.openclaw/workspace）
OPENCLAW_WORKSPACE_DIR=./data/workspace

# ---------- 端口 ----------
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790
OPENCLAW_CANVAS_PORT=18793

# ---------- Gateway ----------
# 绑定方式: loopback（仅本机）| lan（局域网，容器推荐）
OPENCLAW_GATEWAY_BIND=lan
# Gateway 访问令牌（建议生成并填入，用于 Control UI 等）
# 生成示例: openssl rand -hex 24
OPENCLAW_GATEWAY_TOKEN=

# ---------- 模型 / OAuth（按需） ----------
# Anthropic / OpenAI 等可通过 ~/.openclaw/openclaw.json 或下方变量配置
# CLAUDE_AI_SESSION_KEY=
# CLAUDE_WEB_SESSION_KEY=
# CLAUDE_WEB_COOKIE=

# ---------- 代理（构建时） ----------
HTTP_PROXY=http://127.0.0.1:7890
HTTPS_PROXY=http://127.0.0.1:7890
ALL_PROXY=http://127.0.0.1:7890
NO_PROXY=localhost,127.0.0.1,::1


